# 🚀 نظام فزاع برو لإدارة الشكاوى والطلبات (Fazaa Pro System)

نظام تقني متكامل ومتقدم صُمم خصيصاً لإدارة عمليات الصيانة، متابعة الشكاوى، وتعيين الفنيين بكفاءة عالية، مع لوحات تحكم تفاعلية وتقارير أداء دقيقة.

---

## ✨ أبرز المميزات

### 🛠️ إدارة العمليات والطلبات
- **نظام تذاكر شامل:** إنشاء ومتابعة طلبات الصيانة والشكاوى بدقة.
- **تحديثات لحظية (Real-time):** ظهور التحديثات (مثل تغيير الحالة أو الفني) فوراً لدى جميع المستخدمين دون الحاجة لتحديث الصفحة.
- **طباعة وتصدير:** قوالب طباعة احترافية لتفاصيل الطلبات والسجلات.

### 👥 إدراة الفنيين والفرق
- **التعيين الذكي:** واجهة متطورة لاختيار وتغيير الفنيين مع البحث المتقدم.
- **تقييم الأداء (KPIs):** نظام تقييم شامل يشمل (الالتزام، الجودة، السلوك) مع رسوم بيانية.
- **الإشراف:** ربط الفنيين بالمشرفين ومتابعة التقييمات.

### 📊 التقارير والإحصائيات
- **لوحة تحكم (Dashboard):** عرض إحصائيات حية عن الطلبات المغلقة، المفتوحة، والأكثر نشاطاً.
- **سجلات التتبع (Logs):** سجل كامل لكل حركة تتم في النظام (من قام بالتعديل ومتى).

---

## �️ التقنيات المستخدمة (Technology Stack)

تم بناء النظام باستخدام أحدث التقنيات لضمان السرعة والأمان وقابلية التوسع:

### **الواجهة الأمامية (Frontend):**
- **React.js**: لبناء واجهات مستخدم تفاعلية وسريعة.
- **TypeScript**: لضمان جودة الكود وتقليل الأخطاء البرمجية.
- **Vite**: أداة بناء فائقة السرعة.
- **Tailwind CSS**: لتصميم عصري ومتجاوب مع جميع الشاشات.
- **Shadcn UI & Radix UI**: مكتبات مكونات جاهزة لتجربة مستخدم احترافية.
- **React Query**: لإدارة جلب وتحديث البيانات من السيرفر بكفاءة.

### **الواجهة الخلفية (Backend):**
- **Node.js**: بيئة تشغيل جافاسكريبت قوية وسريعة.
- **Express.js**: إطار عمل خفيف ومرن لبناء الـ API.
- **TypeScript**: مستخدمة أيضاً في السيرفر لتوحيد اللغة وكتابة كود آمن.
- **WebSocket (ws)**: لدعم التحديثات اللحظية والإشعارات الفورية.

### **قواعد البيانات (Database):**
- **SQLite**: قاعدة بيانات خفيفة وسريعة وموثوقة.
- **Drizzle ORM**: للتعامل مع قاعدة البيانات بطريقة حديثة وآمنة (Type-safe).

---

## �💻 متطلبات التشغيل (System Requirements)

- **Node.js**: الإصدار 18 أو أحدث.
- **NPM**: مدير الحزم.
- **سيرفر**: VPS يعمل بنظام Linux (Ubuntu/Debian) وعليه `PM2` لإدارة العمليات.

---

## 🚀 دليل التثبيت والتشغيل المحلي (Local Development)

للبدء في تطوير أو تشغيل النظام على جهازك الشخصي:

1. **تثبيت المكتبات:**
   ```bash
   npm install
   ```

2. **تشغيل وضع المطور:**
   ```bash
   npm run dev
   ```
   سيعمل النظام على الرابط: `At http://localhost:5000`

---

## 📦 دليل البناء والرفع للسيرفر (Deployment Guide)

هذه الخطوات ضرورية عند اعتماد التعديلات ورفع نسخة جديدة للسيرفر.

### 1️⃣ بناء المشروع (Build)
قم بتنظيف النسخ القديمة وبناء نسخة جديدة لضمان خلوها من الأخطاء:

**لأنظمة Windows (Powershell):**
```powershell
# حذف مجلد البناء القديم لضمان نظافة النسخة
Remove-Item -Recurse -Force dist

# بناء المشروع
npm run build
```

### 2️⃣ ضغط الملفات
بعد انتهاء البناء بنجاح، قم بضغط مجلد `dist` الناتج لسهولة رفعه:
```powershell
tar -czf dist.tar.gz dist
```

### 3️⃣ رفع الملف
قم برفع ملف `dist.tar.gz` إلى رابط مباشر (يمكنك استخدام خدمات مثل `tmpfiles.org` للسرعة) أو رفعه عبر `SCP/FTP` إلى السيرفر مباشرة.

---

## 🔄 دليل تحديث السيرفر (Server Update)

لتطبيق التحديثات على السيرفر الحي، اتبع الخطوات التالية:

1. **دخول السيرفر:** اتصل بالسيرفر عبر SSH.
2. **تنفيذ أوامر التحديث:**

إذا كنت قد أنشأت ملف التحديث السريع `update.sh`:
```bash
./update.sh
# ثم الصق رابط الملف الجديد عند الطلب
```

أو يدوياً عبر الأوامر التالية:
```bash
cd /root/fazaa-system

# استبدل LINK_HERE برابط الملف الجديد
wget -O dist.tar.gz "LINK_HERE"

# فك الضغط واستبدال الملفات
tar -xzf dist.tar.gz

# إعادة تشغيل النظام لتطبيق التغييرات
pm2 restart all
```

---

## 📂 هيكلية المشروع

- **`client/`**: كود الواجهة الأمامية (React, Vite, Tailwind).
- **`server/`**: كود الواجهة الخلفية (Node.js, Express).
- **`shared/`**: الأنواع (Types) والمخططات المشتركة بين السيرفر والعميل.
- **`dist/`**: النسخة النهائية الجاهزة للرفع (تنتج بعد عملية الـ Build).

---

**تم التطوير بواسطة:** ENG Youssef EL captain
**الإصدار:** 1.0.0 Pro
